 // temp = tail;
    // tail = head;
    // head = temp;
    // for (int i = 1; i < count; i++)
    // {
    //     vol=temp->prev;
    //     temp->prev = temp->next;
    //     temp->next = vol;
    //     temp = temp->next;
    // }
    // temp = head;
    // while (temp != 0)
    // {
    //     printf("%d \n", temp->data);
    //     temp = temp->next;
    // }